pool:
  name: Build
  demands: Agent.OS -equals Linux

trigger: none

stages:
    
pool:
  name: Build
  demands: Agent.OS -equals Linux

trigger: none

stages:
    
# Build
# -----
  - stage: BuildStage
    displayName: Build (Docker)
    dependsOn:
    jobs:
    - template: ci/jobs/build-docker.yml@templates
      parameters:
        imageName: maib-obp-api
        buildDir: $(Build.SourcesDirectory)/Dockerfile